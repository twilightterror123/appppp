# NEW AI knowledge / training data

This folder is a **retrieval/behavior seed**, not a copy of any proprietary model's training data.

Use it to build a RAG layer or fine-tuning dataset with content you own or are licensed to use. Do not scrape or copy private/proprietary training corpora.

The server currently uses the file as project documentation; a future RAG worker can index it into a vector database.
